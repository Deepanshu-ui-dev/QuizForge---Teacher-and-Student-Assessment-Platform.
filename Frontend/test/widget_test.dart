import 'package:flutter_test/flutter_test.dart';
import 'package:quiz_platform/app/app.dart';

void main() {
  test('QuizPlatformApp can be constructed', () {
    expect(const QuizPlatformApp(), isA<QuizPlatformApp>());
  });
}
